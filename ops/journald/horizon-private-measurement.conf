# Temporary opt-in drop-in for exactly the two private real-pack measurement
# boots. Remove it before any production boot; the base namespace is limited.
[Journal]
RateLimitIntervalSec=0
RateLimitBurst=0
