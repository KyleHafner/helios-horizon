# Documentation-only H1/H2/G11 reference configuration.
# This file is not consumed by the baseline installer.
profiles_dir = "/etc/game-control/profiles.d"
state_db = "/var/lib/game-control/state.db"
reservation_path = "/run/game-control/reservation.json"
boot_profile = "resume-last-active"
boot_autostart = false

[[schedule]]
cron = "0 3 * * *"
profile = "minecraft-sunlit-cobblemon"
backup_destination = "example-b2"

[stats]
exporter_url = "http://127.0.0.1:19565/metrics"
tps_interval_seconds = 30

[rcon]
host = "127.0.0.1"
port = 25575
password_path = "/run/credentials/horizon-rcon-password"

[crafty]
base_url = "https://127.0.0.1:8443"
token_path = "/run/credentials/horizon-crafty-token"
verify = true
